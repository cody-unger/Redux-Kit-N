import React from 'react';

export default (
  <div>
    Click the + next to the section header to add a component!
    <br />
    <br />
    -To attach a component to <b>App</b>, or to any other component, simply
    click, drag and drop that component onto the specific component you’d like
    to render it!  You can drop in either the <b>Components</b> section or the
    <b>App Tree.</b>
    <br />
    <br />
    -All components must be picked from the components section.
    <br />
    <br />
     -Hover over a component to see the tools for that component.
    <br />
    <br />
    You will be able to expand or hide the child components, edit the name of the component,
    and delete the component (on all but App).
  </div>
);
